# Multithreaded-TCP-Server
A multithreaded concurrent TCP server implemented in C using POSIX Threads (pthreads). Handles multiple client connections simultaneously to perform server-side sorting of integer streams without blocking.
